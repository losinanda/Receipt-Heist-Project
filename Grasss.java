import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Land1 here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Grasss extends Land
{
    public Grasss()
    {
        GreenfootImage image = getImage();
        image.scale(image.getWidth(), image.getHeight()/1);
        setImage(image);
    }
    
    public void act() 
    {
        // Add your action code here.
    }    
}
